"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatIssues = void 0;
var formatIssues = function (issues, config) {
    if (issues === void 0) { issues = ''; }
    if (issues.length === 0) {
        return '';
    }
    var hasCommas = /,\s/.test(issues);
    var issuesItems = hasCommas ? issues.split(', ') : issues.split(' ');
    if (config.issuesPrefix) {
        try {
            var href_1 = new URL(config.issuesPrefix).href;
            issuesItems = issuesItems.map(function (issue) { return href_1 + issue; });
        }
        catch (error) {
            issuesItems = issuesItems.map(function (issue) { return config.issuesPrefix + issue; });
        }
    }
    return 'See issues: ' + issuesItems.join(', ');
};
exports.formatIssues = formatIssues;
